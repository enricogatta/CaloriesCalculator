# Configurazione per `netlify dev` eseguito dalla cartella frontend/
# (il netlify.toml nella root è usato da Netlify in produzione)

[functions]
  directory = "../netlify/functions"

[[redirects]]
  from   = "/api/*"
  to     = "/.netlify/functions/:splat"
  status = 200
